function getLength(arr) {
    return arr.length
}
function getFirst(arr) {
    var one = arr[0]
    return one
}
function getLast(arr) {
    var neone = arr[arr.length - 1]
    return neone
}
function pushElement(arr) {
    var el = 1;
    arr.push(el)
    return arr
}
function popElement(arr) {
    arr.pop()
    return arr
}